{
    "scripts": {
        "start": "concurrently 'browser-sync start -c bs-config.js' 'web-ext run' --raw"
    },
    "devDependencies": {
        "browser-sync": "^2.26.7",
        "concurrently": "^4.1.2",
        "web-ext": "^3.1.1"
    }
}
